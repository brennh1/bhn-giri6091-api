services:
  postgres-db:
     image: postgres
     container_name: postgres
     restart: always
     ports:
       - 54320:5432
     environment:
        - POSTGRES_DB=bhn_db
        - POSTGRES_USER=admin
        - POSTGRES_PASSWORD=admin123
